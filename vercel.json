{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && npx prisma generate --schema src/prisma/schema.prisma && cd src && npm install",
  "buildCommand": "touch .env && npm run build && cp -r src/.next .next && cp -r src/node_modules/.prisma node_modules/ && cp -r src/node_modules/@prisma node_modules/",
  "regions": ["icn1"],
  "crons": [
    {
      "path": "/api/v1/drive/cron",
      "schedule": "0 0 * * *"
    }
  ]
}
