{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "rootDirectory": "src",
  "installCommand": "npm install && npx prisma generate --schema src/prisma/schema.prisma && cd src && npm install",
  "buildCommand": "touch .env 2>/dev/null || true && npm run build",
  "regions": ["icn1"],
  "crons": [
    {
      "path": "/api/v1/drive/cron",
      "schedule": "0 0 * * *"
    }
  ]
}
