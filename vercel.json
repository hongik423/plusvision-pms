{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install && npm run db:generate",
  "buildCommand": "touch .env 2>/dev/null || true && npm run build",
  "regions": ["icn1"],
  "crons": [
    {
      "path": "/api/v1/drive/cron",
      "schedule": "0 0 * * *"
    },
    {
      "path": "/api/v1/documents/purge",
      "schedule": "0 3 * * *"
    }
  ]
}
